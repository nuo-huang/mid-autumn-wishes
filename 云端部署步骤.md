# GitHub Pages + Supabase

这是无需自建服务器的方案：GitHub 存代码和发布网页，Supabase 存留言并运行提交接口。已上线：https://nuo-huang.github.io/mid-autumn-wishes/ 。代码仓库：https://github.com/nuo-huang/mid-autumn-wishes 。未开通付费服务。

当前项目 ntzhswldgbibdcxmeojw 位于新加坡，数据库和 guestbook 云函数已部署；两个 GitHub 仓库变量及 Pages 发布已配置。允许来源为 https://nuo-huang.github.io 。以下步骤保留供重新部署或迁移使用，不需要重复创建项目。

## 1. 创建 Supabase 项目

在 Supabase 创建一个专用项目，等待数据库就绪。记下项目 URL（`https://项目编号.supabase.co`），并在项目 API Keys 页面取得 **publishable key**（以 `sb_publishable_` 开头）。这些是前端公开配置，不能填数据库密码或 service_role/secret key。

在 SQL Editor 中执行 `supabase/migrations/202609170001_guestbook.sql`。它创建留言表、读取/写入函数和访问规则，不删除已有留言。无需将表向匿名访客开放；访客通过 Edge Function 访问。

## 2. 发布云函数

在本目录使用 Supabase 官方 CLI 登录并关联你的项目：

```sh
supabase login
supabase link --project-ref 你的项目编号
supabase secrets set ALLOWED_ORIGINS=https://你的用户名.github.io
supabase functions deploy guestbook --no-verify-jwt
```

如使用自定义域名或要允许本地测试，用逗号分隔完整 origin，例如 `https://example.com,http://127.0.0.1:8787`。不要带仓库路径或尾斜杠；GitHub 项目网址即使有 `/仓库名/`，origin 仍然是 `https://你的用户名.github.io`。

函数运行所需的 `SUPABASE_URL` 与 `SUPABASE_SERVICE_ROLE_KEY` 由 Supabase 托管环境提供。管理密钥只能在云函数环境中使用，不能填进前端或提交进 GitHub。此公共留言功能不要求访客登录，因此部署时关闭 JWT 验证；函数仅允许配置的浏览器来源，数据库仅允许服务端函数角色访问。

浏览器来源限制并不是访客身份认证，非浏览器程序仍可伪造 Origin。此版本适合小范围分享，数据库中另设全站每分钟30条、总计10000条的硬上限。公开大规模使用前，可增加验证码、账号登录或审核机制。

## 3. 填写前端配置

修改 `frontend/config.js`：

```js
window.MOON_CONFIG = Object.freeze({
  provider: 'supabase',
  supabaseUrl: 'https://你的项目编号.supabase.co',
  publishableKey: 'sb_publishable_你的公开密钥'
});
```

在本机可以继续使用 `provider: 'local'` 配合 Node/SQLite。切换配置不修改数据库，也不会自动迁移本地留言。

## 4. 发布到 GitHub Pages

将本交付目录内容放在 GitHub 仓库根目录；不要提交 `data/`、SQLite 文件、私钥或工作目录的测试数据库。

工作流已放在 `.github/workflows/pages.yml`，无需手动复制。在仓库 Settings → Pages 中把 Source 设为 GitHub Actions。默认工作流使用 `main` 分支；如果你的默认分支不同，修改工作流中的分支名。

在 Settings → Secrets and variables → Actions → Variables 中新增 `SUPABASE_URL` 和 `SUPABASE_PUBLISHABLE_KEY` 两个仓库变量，分别填写项目地址和公开密钥。发布流程会自动生成云端前端配置；本地 `frontend/config.js` 可以继续保留 local 模式。工作流不会接受 secret/service_role 密钥。

填好两个变量后，在 Actions → Publish moonlight frontend → Run workflow 手动启动第一次发布，之后推送 main 会自动发布。缺少变量时部署任务跳过。工作流会先运行 `npm test`，再生成云端配置和离线贺卡，仅发布 `frontend/` 目录；测试失败则停止发布。后端源代码不会作为网页目录发布，数据库一直留在 Supabase。当前代码仓库公开，不包含真实留言数据库和私密凭证。

## 5. 上线后检查

1. 第一部手机提交一条测试祝福。
2. 第二部手机打开同一网址，点击“刷新留言”，应出现同一条留言。
3. 修改一次配色并重新发布，旧留言应仍然存在。
4. Supabase Table Editor 中可以查看、管理留言；本项目没有另建管理员页面。

已完成真实 Supabase SQL 执行、Edge Function 读写、浏览器跨域请求和 GitHub Actions 发布。上线测试留言由网页提交，并通过新页面和独立客户端读回；匿名直接访问留言表及 RPC 被拒绝。尚未进行两部实体手机测试。修改前端并推送 main 后自动部署，不会执行数据库迁移或清空已有留言。

提供商的免费额度、休眠和价格以当前官方控制台为准；本交付没有开通任何付费服务。
