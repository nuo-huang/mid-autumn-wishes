# 前端可以独立修改

**改配色、排版、文字、月亮、孔明灯、云雾，不需要改数据库，也不会删除留言。**

| 想改什么 | 修改文件 |
| --- | --- |
| 页面结构、按钮、默认祝福 | `frontend/index.html` |
| 颜色、字体、手机布局 | `frontend/styles.css` |
| 灯笼数量/速度、云雾、倒计时、爱心 | `frontend/animations.js` |
| 留言列表、表单、成功提示 | `frontend/guestbook.js` |
| 本地/云端切换、公开连接配置 | `frontend/config.js` |
| 留言请求的实现 | `frontend/message-api.js` |
| 本地后端 | `server.cjs` |
| 云端校验接口 | `supabase/functions/guestbook/` |
| 云数据库建表与写入规则 | `supabase/migrations/` |

修改前端后运行 `node build.cjs`，更新独立 HTML 和可下载贺卡。`mid-autumn.html` 和 `frontend/offline-card.html` 是生成文件，不建议直接修改，否则下次构建会覆盖。浏览器刷新即可看到变化，不需要清空数据或重新建表。GitHub Pages 工作流会自动运行构建。

前端固定使用两种操作：读取返回 `{messages:[{id,name,body,createdAt}]}`，提交 `{name,body}` 后返回 `{message:{id,name,body,createdAt}}`。本地接口和 Supabase 云函数遵守相同格式。只改视觉效果时无需了解这些字段。

新增点赞、图片、回复等涉及数据的新功能，才需要扩展接口和表。数据库更改应作为新的迁移脚本添加，保留旧字段兼容性，并先备份；不要删除旧表再重建。

当前本地模式已完成实际数据库写入与重启持久化测试。云端文件已准备并通过本地接口测试，但尚未在真实 Supabase 项目创建表或上线验证。本地 SQLite 数据不会自动搬到 Supabase；后续如有真实留言，应专门做一次数据迁移。
