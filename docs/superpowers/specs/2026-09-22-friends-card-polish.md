# 给朋友的中秋贺卡微调

用户已认可上轮优化建议，并要求直接看效果、保持简单。本轮采用保留现有风格的微调，不新增服务或交互流程。

- 手机缩短首屏标题、场景和正文之间的距离，主按钮更早出现；月亮上下为两行留言保留空间。
- 气泡数量按可用空间决定，手机同时最多三条、桌面最多五条；每条成功摆放后展示十秒，未能放下的内容回到待展示队列。
- 保留自动玉兔开场、灯雾循环、共享留言、原有专属贺卡编辑和导出。入口命名区分个人贺卡和公共留言。
- 背景降低亮度与文字雨强度，提高阅读文字的字号，使用已有原生 HTML/CSS/JavaScript。
- 验证构建、现有测试、新增队列重试回归；浏览器检查手机/窄屏/桌面排版、长留言、开场和编辑流程，并保存效果截图。先交付本地预览。

验证完成：npm test 全部通过；Edge 浏览器 320×740、390×844、1280×800 长留言排版通过，分别观察到 2、3、2 条可读气泡，无互相遮挡或横向溢出。390×844 主按钮底部约 743px。连续 30 帧的移动与透明度均逐帧变化；滚动、缩放、编辑、自动开场和减少动态效果检查通过。

本地预览：http://127.0.0.1:8797/；示例留言仅存储在 work/friends-preview.sqlite。手机及电脑效果截图位于 output/playwright/friends-*.png。未更新线上 GitHub Pages。
