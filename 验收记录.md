# 项目验收记录

本交付含全屏动画、前后端分离、SQLite 本地后端、Supabase 云数据库和 GitHub Pages 工作流。**已上线：https://nuo-huang.github.io/mid-autumn-wishes/ 。**

已完成：

- 浏览器验证全屏动画、手机尺寸、暂停/恢复、贺卡编辑和离线导出。
- SQLite 真实写入、另一客户端读取、重启持久化、请求校验、限流和文件隔离。
- 前端旧读取与新提交并发测试；避免旧列表覆盖新留言，网络同步失败时保留已提交留言。
- 云函数在本地测试请求格式、CORS、错误响应和公开连接配置。
- Supabase SQL 已在本地 PGlite PostgreSQL 引擎执行，验证建表、函数调用、重复迁移保留数据、匿名访问被拒绝、服务角色读写和数据库限流。
- `npm test` 全部通过；实际浏览器无 error/warn。交付包未包含测试数据库或秘密凭证。

2026-09-17 真实云端验证：

- Supabase 项目 ntzhswldgbibdcxmeojw（新加坡），SQL 建表和角色权限执行成功。
- guestbook Edge Function 部署成功，ALLOWED_ORIGINS 为 https://nuo-huang.github.io 。
- 云端检查通过：读取 200、跨域预检 204、非法来源 403、空昵称 400；匿名直接访问留言表和 RPC 被拒绝。
- GitHub Pages 发布运行 35239337210 成功，包括 Linux / Node 24 下 npm test、配置生成、构建和 HTTPS 部署。
- 正式网页提交“上线测试”留言成功，数据库记录 id=1；新页面及独立 Node 客户端均读到同一条记录。网页显示“祝福已保存，大家都能在这里看到”。正式网页控制台无 error/warn。
- 未进行两部实体手机测试。在线共享留言依赖网络及托管服务可用性，下载的离线贺卡不包含公共留言。

2026-09-17 云端进展：GitHub 与 Supabase 已登录，GitHub CLI 认证已恢复。完整源码已上传至 https://github.com/nuo-huang/mid-autumn-wishes ，Pages 已设为 GitHub Actions。云端验证工作流 Check project 成功（运行 35235815190），确认 Linux / Node 24 环境下 npm test 全部通过。

Supabase 免费组织及项目已创建，未开通付费服务。后续修改前端并推送 main 会自动发布，不会执行数据库迁移或清空留言。
