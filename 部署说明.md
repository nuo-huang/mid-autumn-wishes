# 中秋祝福网站 · 共享留言版

页面包含全屏孔明灯、云雾、祝福编辑和公共留言板。默认本地模式使用 SQLite；另附 Supabase 云端接入版本，见“云端部署步骤.md”。前端修改方法见“前后端修改指南.md”。

已部署的在线版本：https://nuo-huang.github.io/mid-autumn-wishes/ ，留言保存于 Supabase。下面的 Node/SQLite 步骤用于本地运行或另行部署自有服务器。

## 在电脑上启动

安装 Node.js 24 LTS，在本目录打开终端，执行：

```sh
node server.cjs
```

打开 http://127.0.0.1:8787 。无需安装第三方依赖。默认数据库位于本目录的 `data/messages.sqlite`，首次启动自动创建。

Windows 也可直接双击 `启动网站.bat`。保持窗口开启，网站才能继续提供留言服务。

只双击 HTML 是离线贺卡模式，不连接留言数据库。页面的“保存这份祝福”保存贺卡文字，不备份公共留言。

## 同一 Wi-Fi 下用手机访问

在 PowerShell 中先设置监听地址，再启动：

```powershell
$env:HOST='0.0.0.0'
node server.cjs
```

手机与电脑连接同一 Wi-Fi，访问 `http://电脑的局域网IP:8787`。电脑需开机并保持服务运行；如系统提示，允许该服务通过私人网络防火墙。`127.0.0.1` 只能供当前电脑使用，不能作为发给朋友的公网链接。

## 公网部署

若使用默认 Node/SQLite 模式，需要一台支持 Node.js 24、可长期运行且磁盘持久化的服务器，或支持 Docker 和持久卷的平台。部署服务器与数据库后，把同一个 HTTPS 地址发给朋友。使用 GitHub Pages + Supabase 时按“云端部署步骤.md”操作即可。

可在服务器上构建并启动 Docker：

```sh
docker build -t mid-autumn .
docker volume create mid-autumn-data
docker run -d --name mid-autumn --restart unless-stopped -p 127.0.0.1:8787:8787 -v mid-autumn-data:/data mid-autumn
```

通过服务器上的 Nginx/Caddy 等反向代理配置域名、HTTPS，并代理到 `127.0.0.1:8787`。域名、服务器及 TLS 配置取决于所选平台；当前线上版本使用 GitHub Pages + Supabase，无需运行此 Node 服务。

也可直接使用 Node 启动，设置 `HOST`、`PORT`、`DB_PATH`，并使用服务管理器保持运行。反向代理需保留原始 Host。默认按连接 IP 限制一分钟6次提交；经反向代理的访客会共享代理地址的额度，正式公开运营前应按可信代理配置访客限流。

不要把 SQLite 文件放在临时容器目录、静态网站目录或每次部署都会清空的磁盘上。纯静态托管不能运行此数据库服务。此方案适合单个服务实例；多个实例应接共享数据库服务。

## 留言与数据

- 留言公开显示，昵称24字以内、正文280字以内。最近50条展示，旧留言仍保存在数据库中；访客可点“刷新留言”，页面也会每30秒自动同步。
- 只保存昵称、正文和服务器时间；不保存 IP。限流记录在内存中，重启清空。
- 采用参数化 SQL，网页以纯文本呈现留言。请求大小有上限，并限制跨站提交。
- 这是无需登录的公共留言板，没有账号身份校验、审核和后台删除界面。适合小范围分享；公开运营可继续补充审核与管理功能。
- 最多保存10000条，不会自动删除旧留言；达到上限暂停接收新留言。
- 备份前停止服务，然后备份整个 `data/` 目录（Docker 版本为持久卷内容），完成后重启。不要只在运行时复制主 SQLite 文件而忽略 WAL。

本次交付已包含网站和数据库代码，本地验证数据放在工作目录中；正式部署数据库首次启动为空。

## GitHub 与云数据库怎么选

GitHub 存代码，GitHub Pages 托管静态网页，不运行本项目的 Node.js 服务或 SQLite 数据库。不要把留言数据库或数据库管理密钥提交到 GitHub。

现已完成 GitHub Pages + Supabase（PostgreSQL 云数据库）部署。线上构建使用仓库变量生成云端配置；下载包的默认配置仍连接随附的 Node.js/SQLite 服务，便于本地独立开发。具体部署和迁移步骤见“云端部署步骤.md”。

云版建议允许访客读取已公开留言，通过云函数校验和限制提交频率。数据库管理密钥只保存在服务器或云函数的环境变量里，不写进网页。Supabase 的公开 publishable key 在配置正确的访问策略后可用于前端，service_role 等管理密钥不可暴露。服务额度、休眠与价格以提供商当前方案为准。
